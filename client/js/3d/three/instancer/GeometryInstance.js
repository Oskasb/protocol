"use strict";


define([
        '3d/three/instance/InstanceBuffer'


    ],
    function(
        InstanceBuffer
    ) {

        var GeometryInstance = function() {};


        GeometryInstance.prototype.setPosition = function(position) {

        };

        GeometryInstance.prototype.setQuaternion = function(workerKey, callback) {

        };

        GeometryInstance.prototype.setScale = function(protocolKey, data) {

        };

        GeometryInstance.prototype.showHide = function(bool) {

        };


        return GeometryInstance;
    });

